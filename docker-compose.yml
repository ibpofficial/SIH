version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: freightiq-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-freightiq}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-freightiq_secret_2026}
      POSTGRES_DB: ${POSTGRES_DB:-freightiq_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-freightiq} -d ${POSTGRES_DB:-freightiq_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
